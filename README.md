# TODO-APP
A Todo App built using HTML, CSS, and JavaScript that allows users to add, delete, and mark tasks as completed. The application provides a simple and interactive interface for managing daily tasks and demonstrates the use of DOM manipulation, event handling, and JavaScript array methods such as `map()`, `filter()`, and `forEach()`.
